# FRONTEND service on Railway — Docker container (Next.js)
# Settings → Root Directory MUST be: frontend

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
